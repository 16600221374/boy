# Tomcat
server:
  port: 9000

# Spring
spring:
  application:
    # 应用名称
    name: eureka-demo
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
    register-with-eureka: false
    fetch-registry: false